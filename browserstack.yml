userName: ${BROWSERSTACK_USERNAME}
accessKey: ${BROWSERSTACK_ACCESS_KEY}

framework: mocha

platforms:
  - os: Windows
    osVersion: 11
    browserName: Chrome
    browserVersion: latest

  - os: Windows
    osVersion: 11
    browserName: Firefox
    browserVersion: latest

parallelsPerPlatform: 1

buildName: browserstack-build
projectName: CI-CD-Testing

testObservability: true